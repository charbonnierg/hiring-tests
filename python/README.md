Install pipenv and dependencies
=====================

    pip install pipenv
    pipenv shell
    pipenv install


Test with pytest
=====================

Install the pytest

	pip install pytest

And execute:

	pytest

Any test source matching the pattern test_*.py will be executed.
